# Purpose

Implement each task specified below

## Task 1: Create an 'env' sub-command in the bin/sb script

- The command should done of the following:

  - Use the same logic as the existing sub-commands to determine the active sandbox's root directory
  
  - Print the contents of the .env file in the sandbox's root directory if it exists.

  - Print an error if the .env file does not exist in the sandbox's directory
