This .sb directory "hides" the sandbox project .sb directory in the host for the following reasons:

- Security reasons

- To prevent unintentional execution of `sb` comamnds from within the sandbox

- To present unintentional updates to sandbox/project configuratiob files
